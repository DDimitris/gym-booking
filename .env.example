# Copy this file to .env and adjust values to switch between IP and domain
# These values are used by docker-compose.yml and docker-compose.https.yml

# Public host users will hit in their browser (domain preferred, IP as fallback)
PUBLIC_HOST=localhost

# Scheme served to users: https only (stack is HTTPS-first)
PUBLIC_SCHEME=https
